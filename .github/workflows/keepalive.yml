name: Keep Alive

on:
  schedule:
    - cron: "*/5 * * * *"  # every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render App
        run: curl -fsS https://career-ai-foom.onrender.com/healthz > /dev/null
